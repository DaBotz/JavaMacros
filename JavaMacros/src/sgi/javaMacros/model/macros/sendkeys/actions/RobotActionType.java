package sgi.javaMacros.model.macros.sendkeys.actions;

public enum RobotActionType {
	DELAY, GROUP_OPEN, GROUP_CLOSURE, TYPE_A_KEY, ASYNC, MONODIRECTIONAL, MODIFIER, WAIT, ESCAPE_SEQUENCE
	;

}
